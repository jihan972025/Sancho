### pyshorteners — URL Shortener

Shorten long URLs using TinyURL.

**Parameters:**
- `url` (string, required): The URL to shorten

**Example:**
```
[SKILL_CALL]{"skill": "pyshorteners", "params": {"url": "https://www.example.com/very/long/path/to/something"}}[/SKILL_CALL]
```
